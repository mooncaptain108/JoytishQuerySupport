 - add a menu item that only appears when a chart is displayed.
 - does not appear when reviewing muhurta charts.
 - New menu item with subitems as follows:
   - View
     - Chart
     - Transits
     - Chart & Transits
- the Chart view is identical to the chart we show now.
- Transits View
  - when the Transits view or the Chart & Transits view is selected a data/time/location popover will appear with controls like the ones in new chart.
  - the popover is moveable
  - the Transits view uses the same lagna as the Chart view but displays the current transit positions as specified in the popover dialog.
  - initially the time selected is the current date and time and the location is the location from the current chart.
  - each time any of the control values changes the transit positions should be recalculated and displayed.
  - the transit view chart looks like the Main chart except with transit positions. i.e. same ascendant as main chart, same malefic and benefic colors and letter identifiers. transit identifiers should reflect transit afflictions and combust state.
- the analyze button can be applied to the transit view in the same way it is used for the Chart view.
- Transit & Chart view
  - When Transit & Chart view are selected the current state of the popover settings are applied.
  - Now we will be showing both Chart and Transit view positions for all the planets.
  - For now arrange the planets in a single line in Degree Order for each house where planets are located.
  - Using the same display characteristics from the chart and transit views.
  - However, for the transit planets we will prefix the identifier with a lowercase t colored black.
  - Let's try aligning the identifiers as if there is a blank letter the width of lowercase t prefixed to the Main Chart identifiers
  - in this way lowercase t appears to the left of the column of planets.
- Analyzing Transit & Chart view
  - When the analyze button is clicked for Transit & Chart view
    - we won't do house strength. Only planet analysis
    - we won't calculate planet strength only report conjunctions and aspects within 5 degrees.
    - there will be two sections.
      - transit over natal chart
        - for each planet in the natal chart the analysis should report all transit aspects and conjunctions and the orb of influence.
      - natal chart over transit.
      - for each planet in the tranit chart the analysis should report all natal aspects and conjunctions and the orb of influence.
 


 